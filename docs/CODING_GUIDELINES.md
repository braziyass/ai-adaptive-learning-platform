Always use

Clean Architecture

Repository Pattern

Dependency Injection

DTOs

Services

Never place SQL inside routes.

Never duplicate code.

Use TypeScript everywhere.

Use SQLAlchemy 2.

Use Alembic.

Use JWT.

Every endpoint must have validation.

Every API response must have schemas.

Every component must be reusable.